// I copied these keywords from the spec
// ATTENTION: This file scanning correctly means nothing since the scanner might think they are identifiers
// We really need to run `tokens` on this and check that it produces no identifiers

break        default      func         interface    select
case         defer        go           map          struct
chan         else         goto         package      switch
const        fallthrough  if           range        type
continue     for          import       return       var

print println append
